# ---- Build dell'app Android (Capacitor) ----
# URL del server per il multiplayer. Va impostato QUI prima di `pnpm cap:sync`.
# Senza, l'app parte in single player offline e il multiplayer non si connette.
VITE_SERVER_URL=https://boggle-it-server.up.railway.app

# Dizionario: NON serve più al client (le parole arrivano dalle schede incluse).
